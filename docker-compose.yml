services:
  web:
    image: nginx:alpine
    restart: unless-stopped
    ports:
      - 80
    volumes:
      - ./site:/usr/share/nginx/html
    networks:
      - nginx-network

networks:
  nginx-network:
    external: true